import { css } from "styled-components"

const body = css`
	body {
		overflow: var(--disable-scroll);
		font-size: 16px;

		min-height: 100vh;

		width: 100%;
		max-width: 100vw;
	}
`

export default body
